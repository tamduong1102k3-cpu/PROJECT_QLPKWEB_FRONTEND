# Bỏ qua thư viện node_modules
node_modules/
frontend/node_modules/

# Bỏ qua frontend (chỉ giữ backend trên git)
frontend/

# Bỏ qua các thư mục build đầu ra
dist/
frontend/dist/
backend/target/

# Bỏ qua file cấu hình môi trường và cài đặt IDE
.env
.env.local
.vscode/
.idea/
.codegraph/

# Bỏ qua các file nén đề phòng (tránh làm nặng kho lưu trữ)
*.rar
*.zip
*.tar.gz